import { ChatMain } from "./components/ChatMain";

export default function App() {
  return (
    <main className="min-h-screen flex items-center justify-center p-6 bg-linear-to-br from-blue-100 to-indigo-100">
      <ChatMain />
    </main>
  );
}